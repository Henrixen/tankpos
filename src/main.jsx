import React from "react"
import ReactDOM from "react-dom/client"
import TankPos from "./TankPos.jsx"
import "./index.css" 

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TankPos />
  </React.StrictMode>
)
